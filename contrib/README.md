# Contrib

Optional and additional files for Pi in a Box live here.

- [`APPEND_SYSTEM.md`](APPEND_SYSTEM.md): example Pi system-prompt append file that tells the agent about the containerized environment.
